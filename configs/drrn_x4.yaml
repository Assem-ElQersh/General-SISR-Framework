defaults:
  - base

experiment:
  name: "drrn_x4"

model:
  name: "drrn"
  scale: 4
  in_channels: 1
  out_channels: 1
  num_features: 128
  num_blocks: 1
  num_units: 25

data:
  scale: 4
  patch_size: 32
  batch_size: 32

trainer:
  max_epochs: 200

optimizer:
  lr: 1.0e-4

scheduler:
  T_max: 200
